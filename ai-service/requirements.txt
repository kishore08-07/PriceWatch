# PriceWatch AI Service — Python Dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# ML / NLP
transformers==4.47.1
torch==2.5.1           # CPU-only; use torch==2.5.1+cu121 for CUDA
sentencepiece==0.2.0   # Required by some tokenizers
protobuf==5.29.2       # Required by sentencepiece

# Text preprocessing
emoji==2.14.0          # Emoji → text conversion
langdetect==1.0.9      # Language detection — filters non-English reviews before DistilBERT

# Keyphrase extraction (Phase 3 — dynamic pros/cons)
keybert==0.8.5         # KeyBERT: semantic keyphrase extraction from review text
scikit-learn>=1.4.0    # TF-IDF (used by KeyBERT internally + for extractive pre-selection)

# Validation
pydantic==2.10.3
